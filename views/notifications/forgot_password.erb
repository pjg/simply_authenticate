Twoje hasło w serwisie <%= SimplyAuthenticate::Settings.notifications[:application] %> zostało zmienione. Nowe hasło to:

<%= @password %>

Zaloguj się i zmień je na łatwiejsze do zapamiętania:
<%= login_url(:host => SimplyAuthenticate::Settings.notifications[:host]) %>
